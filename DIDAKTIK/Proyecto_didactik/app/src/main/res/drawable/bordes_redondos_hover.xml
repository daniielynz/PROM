<!-- res/color/bordes_redondos_hover.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el botón está presionado (hover) -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="75dip" />
            <stroke android:width="2dip" android:color="@color/masOscuro" />
            <solid android:color="@color/white" />
            <padding
                android:left="20dp"
                android:top="10dp"
                android:right="20dp"
                android:bottom="10dp"/>
        </shape>
    </item>
    <!-- Estado por defecto o cuando no está presionado -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="75dip" />
            <stroke android:width="2dip" android:color="@color/white" />
            <solid android:color="@color/masOscuro" />
            <padding
                android:left="20dp"
                android:top="10dp"
                android:right="20dp"
                android:bottom="10dp"/>
        </shape>
    </item>
</selector>

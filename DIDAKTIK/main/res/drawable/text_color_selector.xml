<!-- res/color/text_color_selector.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el botón está presionado (hover) -->
    <item android:state_pressed="true" android:color="@color/masOscuro" />
    <!-- Estado por defecto o cuando no está presionado -->
    <item android:color="@color/white" />
</selector>
